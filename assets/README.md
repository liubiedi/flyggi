# Garden model image

Save the uploaded 3D garden render as `garden-model.png` in this folder.

The interactive page uses that image as the exact visual layer and places accessible hotspot buttons over the labelled zones. This keeps the webpage visually identical to the uploaded 3D model while preserving tap/click interactions.
